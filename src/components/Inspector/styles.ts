export const inspectorStyles = {
  root: 'h-full overflow-y-auto border-l border-rule bg-surface',
  header: 'sticky top-0 z-10 border-b border-rule bg-surface px-4 py-3 font-mono text-[9px] font-medium uppercase tracking-[0.18em] text-muted',
} as const;
